# @tanqory/* (theme-kit) is published to GitHub Packages, not the public npm.
# Set NODE_AUTH_TOKEN to a GitHub token with read:packages before installing:
#   NODE_AUTH_TOKEN=<token> npm install
@tanqory:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
